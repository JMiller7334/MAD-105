fun main(){
    var Employee1 = Employee("Jacob", 2198394, 2) // create a class using the employee class.
    Employee1.printInfo() // call the printInfo function from the created class. This will print the info in the output.

    var Employee2 = Employee("kate", 3441446, 1)  // create a class using the employee class.
    Employee2.printInfo() // call the printInfo function from the created class. This will print the info in the output.
}